# MS Dos Frame Buffer lib

This project is intended to be a single header library that provides a drawing API and allows the user to create graphics with different backends (SDL, GLFW, SDL, or even a terminal) the same way was made on the old days when people created games on MS-DOS.
